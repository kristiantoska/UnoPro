export const INITIAL_GAME_STATE = {
  lastCardValue: null,
  boardColor: null,
};

export default function reducer(state, action) {
  switch (action.type) {
    default:
      throw new Error();
  }
}
